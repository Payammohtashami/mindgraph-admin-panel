import Users from '@/containers/Users';
import { useGetUsersList } from '@/http/query';
import React from 'react';

const Page = () => {
    
    return (
        <Users />
    );
};

export default Page;